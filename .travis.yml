os:
  - linux

language: php

php:
  - '7.3'

before_script:
  - echo "TerminusMq WebOffice build..."
  - composer self-update
  - composer install --no-interaction
  - php artisan key:generate
  - nvm install 7.7.1
  - npm install npm@latest -g
  - npm install
  - npm rebuild node-sass

script:
  - npm run production
  - echo "PHP unit tests..."
  - vendor/bin/phpunit --coverage-text

